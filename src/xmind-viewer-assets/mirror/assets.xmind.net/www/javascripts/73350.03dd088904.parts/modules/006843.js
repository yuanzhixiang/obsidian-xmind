export default {
    6843: function (e) {
        'use strict';
        const t = window.Backbone;
        if (!t)
            throw new Error(
                'XMind viewer runtime requires package-provided Backbone.'
            );
        e.exports = t;
    },
};
